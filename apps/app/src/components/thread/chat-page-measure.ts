/**
 * Shared measure for primary chat surfaces. Keep the new-thread home,
 * conversation timeline, composer, and loading silhouette on one width so
 * navigation never makes the workspace jump between unrelated layouts.
 */
export const CHAT_PAGE_MAX_WIDTH_CLASS = "max-w-[980px]";
